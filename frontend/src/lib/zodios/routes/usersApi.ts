import { type ZodiosEndpointError, makeEndpoint, parametersBuilder, makeErrors, apiBuilder } from "@zodios/core";

// You can define multiple errors and compose them in makeErrors

// EXAMPLE [2.ii.a.c] - createUser

// Build and export our usersApi
